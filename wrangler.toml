# Cloudflare Pages configuration
# Build output directory (produced by vite build)
pages_build_output_dir = "./artifacts/cram-ai/dist"

# SPA fallback - Cloudflare Pages handles this automatically for index.html
